import 'package:flutter/material.dart';

class AppIcon{
  const AppIcon._();

  static const arrowForward =  Icon(Icons.arrow_forward_ios);
  static const arrowBack = Icon(Icons.arrow_back_ios);
  static const document = Icon(Icons.document_scanner_outlined);
}